# Working documents (project plan / briefs), not site content
Tech4TIME-Static-Website-Plan_v3.md

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Python (tools/)
__pycache__/
*.py[cod]
.venv/
venv/

# Screenshots from tools/shoot_pages.py — a look-at-it aid, regenerated on
# demand and large. The script is tracked; its output is not.
tools/shots/

# Runtime artefacts from the job post editor. careers.json itself IS tracked
# (it seeds a fresh deploy); its backup and the test harness router are not.
content/careers.json.bak
content/*.tmp
.test-router-*.php
